name: Setup Hugo
description: Hugo(extended)をセットアップする

runs:
  using: composite
  steps:
    - uses: peaceiris/actions-hugo@2752ce1d29631191ea3f27c23495fa06139a5b78 # v3.2.1
      with:
        hugo-version: "0.115.1"
        extended: true
